# proc_macro_allowlist_direct.toml — R-1 S4(b3-direct).
#
# `stsh_token`'s DIRECT proc-macro dependency edges ONLY — walked from
# `stsh_token`'s own `resolve.nodes[].deps` in
# `cargo metadata --format-version 1 --offline --locked`, kind normal-or-build,
# NOT the transitive closure. Pinned by name + version + Cargo.lock checksum.
#
# This is a PURE ALLOWLIST: a set comparison against reviewed data, nothing more.
# It does NOT itself refuse an identifier-synthesizing macro crate that a reviewer
# adds here — that refusal lives at the invocation-name layer, S4(b3-belt),
# independent of this file's content. A reviewed edit to this file is therefore a
# TCB event (CTO_RULING_LINT_TCB_BOUNDARY_2026-09-04, item 1), not a lint bypass.
#
# Note the compile-failure floor: because this is enforced as a pure allowlist,
# a crate absent from `canisters/token/Cargo.toml`'s `[dependencies]` cannot be
# named from token source at all — `extern crate paste`, `use paste` and
# `::paste::paste!` all fail with `error[E0463]: can't find crate for 'paste'` on
# ordinary rustc name resolution, with no lint involved.
#
# Reviewed data. Never auto-derived, never regenerated by the gate.

[[crate]]
name = "ic-cdk-macros"
version = "0.16.0"
checksum = "0d4d857135deef20cc7ea8f3869a30cd9cfeb1392b3a81043790b2cd82adc3e0"
