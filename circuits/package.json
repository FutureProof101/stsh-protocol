{
  "name": "stsh-circuits",
  "version": "0.2.0-a0",
  "description": "STSH ZK circuits — circomlib dependencies and snarkjs tooling",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test:poseidon": "node tests/poseidon_test.js",
    "test:adversarial": "node tests/adversarial.test.js",
    "compile": "circom spend.circom --r1cs --wasm --sym --output ./build",
    "setup:ptau": "echo 'Trusted setup requires ZK engineer sign-off. The M5 ceremony ran over the production domain constant, but its output is a SINGLE-PARTICIPANT DEV VK pending the A6.7/2-8 re-ceremony (AR1-13) — the trusted setup is NOT finished. Do not regenerate artifacts here: the replacement comes from that ceremony. SANCTIONED EXCEPTION — lane A6.6 regenerated the DEV chain in place (compile-circuit + snarkjs groth16 setup against the tracked circuits/pot14_final.ptau) because the circuit source itself changed (MAX_NOTE_VALUE 10^11 -> 10^15, DOMAIN_CIRCUIT_VERSION 2 -> 3); that is a dev-chain re-pin, not the launch ceremony.'",
    "verify:ceremony": "node scripts/verify_ceremony.mjs --generation m5",
    "verify:ceremony:next": "node scripts/verify_ceremony.mjs --generation next",
    "test:ceremony-supersede": "node tests/ceremony_supersede.test.js",
    "verify:poseidon": "node tests/poseidon_test.js"
  },
  "dependencies": {
    "circomlib": "2.0.5",
    "circomlibjs": "^0.1.7",
    "ffjavascript": "0.3.0",
    "snarkjs": "0.7.5"
  },
  "notes": {
    "circomlib": "Pinned to 2.0.5 — matches POSEIDON_PARAMS.md. Do not upgrade without ZK engineer review and circuit re-audit.",
    "snarkjs": "Pinned to 0.7.5 — Groth16 proof generation and verification. Upgrade requires re-testing proof format compatibility.",
    "ffjavascript": "Pinned to 0.3.0 — BN254 field arithmetic. Must match circomlib internal dependency.",
    "trusted-setup": "No trusted-setup artifacts are generated by npm scripts. The M5 ceremony ran over the production domain constant; its output is a single-participant DEV VK pending the A6.7/2-8 re-ceremony (AR1-13), NOT a finished production setup. The current VK is committed at circuits/verification_key.json and is the one that re-ceremony replaces.",
    "verify:ceremony": "Ceremony acceptance-evidence checker (A-3). NOT part of ./run_gate.sh: against the current M5 material check C1 FAILS BY DESIGN (F6-1 — single-entropy ptau), so wiring it into the gate would make the gate permanently red for a known-open finding. It is the A6.7 acceptance command: `verify:ceremony:next` must be all-green before the new ceremony is accepted. See docs/ceremony/PTAU_MULTIPARTY_PLAN.md."
  }
}
